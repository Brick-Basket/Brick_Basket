Isolated mock datasets, one file per entity, matching the TypeScript shapes
in src/types/domain. Never imported directly by presentation components —
only by the matching adapter in src/lib/api/adapters. See ARCHITECTURE.md
section G.
