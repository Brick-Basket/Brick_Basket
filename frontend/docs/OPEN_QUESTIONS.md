# Open Questions / Requires Client or Backend Confirmation

Living list. Nothing here is invented into a business rule — the frontend renders these as clearly-labeled demo/configurable behavior until resolved.

| # | Question | Where it surfaces | Status |
|---|---|---|---|
| 1 | Vendor Series 6 (Design/Detailing/Consultancy): owner text says "60001+" while Series 1–5 are six digits (100001+…500001+). Is "600001+" intended? | Vendor Management (Part 7) | Open |
| 2 | Full role → permission matrix, incl. who holds PO Approval Level 1 vs Level 2 | All internal modules | Open |
| 3 | Exact workflow/status vocabulary beyond the confirmed two-level PO approval (lead pipeline stages, requisition states, contract revision-after-acceptance rules) | Leads, Requisitions, Contracts | Open |
| 4 | Detailed Cost Management module business rules — pending separate Excel spec from Pushkar Tiwari | Cost Management (Part 18) | Blocked on external input |
| 5 | Sample dates (Oct-26–Mar-27) and monetary figures (₹77L budget example etc.) are illustrative only | Schedule, Project Cost | Confirmed as illustrative, not a question |
| 6 | Does MRC customer acceptance happen in the customer portal, admin, or both concurrently? | MRC (Part 12) | Resolved for this build (frontend decision, not owner-confirmed): customer portal only, mirroring Contract's admin-issues/customer-responds split — see #33 |
| 7 | Auth/session model: JWT vs server session, refresh/expiry behavior, password reset flow ownership. Part 3's mock implementation is entirely client-side (localStorage) with no server verification — a real backend needs server-side enforcement (e.g. middleware + httpOnly cookies), not just this client guard. | Auth (Part 3) | Open — backend decision |
| 8 | File storage provider, per-category max file size, allowed file types for drawings/certificates/invoices | Documents (Part 6) | Open |
| 9 | Document versioning and deletion/archive rules | Documents (Part 6) | Open |
| 10 | Notification channels (in-app only vs email/SMS/WhatsApp) and events that trigger them | Notifications (Part 3+) | Open |
| 11 | GRN/Stores access: "Admin/Stores only" — does `store_personnel` get full GRN write access or review-only? | Store Management (Part 11) | Open — this build proceeds with `store_personnel` holding full `grn:create`/`stock:write`/`wastage:write` access (see `permissions.ts`), as a working assumption |
| 12 | Wastage threshold definition ("materials procured above defined scope") — is the scope threshold per-item, per-category, or per-project? | Wastage (Part 11) | Open |
| 13 | Primary brand font "Super Wonder" (per Branding.pdf) is a licensed display face with no available webfont/Google Fonts source. Headings currently render in Baloo 2 as an interim fallback. | Global — `docs/BRAND_GUIDELINES.md` | Open — needs licensed font files from client |
| 14 | Logo assets exist only as a flattened PDF export (raster), no vector source. | Global branding — `public/brand/*` | Open — request native AI/EPS/SVG from designer |
| 15 | No confirmed construction-package tiers or pricing exist. `/plans` is built around the confirmed service list + custom-quote CTA rather than invented tiers/prices. | Plans (Part 2) | Open — supply real package/pricing data if this should become a priced page |
| 16 | FAQ and How It Works copy is frontend-authored draft marketing content (kept deliberately general — no timelines/warranty claims), not owner-supplied. | FAQ, How It Works (Part 2) | Open — needs client review/replacement copy |
| 17 | Production domain for `metadataBase`/Open Graph is a placeholder (`www.brickbasket.co.in`, unverified). | Root layout SEO (Part 2) | Open |
| 18 | No "session expiring soon" warning or silent refresh is implemented — an expired session logs the user out on next poll (≤60s) with no warning. Frontend scoping decision for Part 3, revisit once the real backend session model is known. | Auth (Part 3) | Open — frontend decision, revisit with backend |
| 19 | `ProjectSelector`/`ProjectProvider` scope a customer's visible projects via a mock `customerId` field with no real ownership resolution. Real project↔customer ownership is backend-owned once Project Management (Part 13) and Contract Management (Part 5) exist. | Shell (Part 3) | Open — placeholder until Project entity is real |
| 20 | Lead pipeline Kanban has no drag-and-drop (a per-card status `Select` moves leads between columns instead) — a frontend scoping decision to avoid adding an unverified DnD dependency for a demo interaction, not an owner requirement either way. | Lead Management (Part 4) | Open — confirm if drag-and-drop is actually required |
| 21 | Lead status transitions are unrestricted today (any status → any status) — no state-machine enforcement of which transitions are valid (e.g. `converted` back to `new`). | Lead Management (Part 4) | Open — needs the confirmed pipeline vocabulary from #3 first |
| 22 | Contract `"declined"` status and the admin "withdraw to draft" action are frontend implementation decisions — the owner requirements only name "review/accept," not a decline/revision path. Revising a declined contract resets it to `draft`. | Contract Management (Part 5) | Open — confirm whether decline/revision should exist and how |
| 23 | `ContractLineItem.category` list (`civil`, `electrical`, `plumbing_sanitary`, `mechanical`, `finishing`, `labour`, `design_consultancy`, `other`) is reused ahead of schedule from the Part 15 Finance/Cost Accounting category set for internal consistency — the owner requirements confirm a "predefined category dropdown" but never name the categories. Now shared by three modules: `Contract` (Part 5), `ACEItem` (Part 8), and `CostEntry` (Part 15) — Part 15's own literal category text is a narrower 7-value list omitting `design_consultancy`; this build keeps the single 8-value union across all three for consistency rather than introducing a near-duplicate. | Contract Management (Part 5), ACE (Part 8), Cost Accounting (Part 15) | Open |
| 24 | Warranty Mapping view maps 1 document → 1 warranted item via `Document.warrantyItem`/`warrantyExpiresAt` (free text + a date), not a real warranted-item catalog — the owner requirements ask for the view but don't define its data model. | Documents (Part 6) | Open |
| 25 | No status field was added to `Document` beyond `version: number` — no owner requirement names one, and the plain version counter plus the warranty fields already cover what's needed. Revisit if a real workflow (e.g. draft/published, or approval before customer visibility) turns out to be required. | Documents (Part 6) | Open — frontend decision |
| 26 | `Document` has no delete/archive action — no owner requirement or client confirmation names one, so none was invented. A new version supersedes the old one (kept in history); nothing removes a document outright. | Documents (Part 6), see also #9 | Open |
| 27 | `Vendor.nature` (owner-specified, 3 values: Supply/Service/Service & Supply) and the vendor code series table (6 categories, adding Specialised Work and Machinery Hiring) don't map 1:1. Modeled as two separate fields — `nature` and `codeSeriesCategory` — rather than merging them. Confirm whether these should in fact be the same field, and if so, which 6 (or 3) values are authoritative. | Vendor Management (Part 7) | Open |
| 28 | Vendor "rating summary" is computed as the average across a vendor's *entire* assessment history, not just its most recent assessment — the owner requirements don't say which. Also, the "past work" section's data model (description + value + completion date, optionally linked to a project) is a frontend invention — the owner requirements ask for the section but don't define its fields. | Vendor Management (Part 7) | Open |
| 29 | `PurchaseRequisitionStatus` vocabulary (`draft`/`submitted`/`approved`/`rejected`) and the single-step approval model (one `requisitions:approve` decision, not a two-level approval like PO) are frontend implementation decisions — the owner text only says these states apply "only if configurable" without naming them, and doesn't confirm an approval workflow exists for this module at all (only that a requisition "flows to admin/purchaser"). Editing a rejected requisition resets it to `draft`, mirroring Contract's decline/revise pattern (#22). | Supply Chain: ACE + Requisition (Part 8) | Open |
| 30 | Several RFQ Management decisions are frontend inventions, not owner-confirmed: (a) the two-state `RFQStatus` vocabulary (`draft`/`finalized`) for the named-but-undefined "Approval/readiness state"; (b) vendor selection modeled **per line** rather than once for the whole RFQ, since different lines can have different lowest bidders; (c) a single RFQ-level `taxPercent` rather than a per-line tax; (d) "% savings over ACE," "Tax" and "Total" computed against the **selected** vendor once chosen, falling back to L1 (the lowest quote) beforehand; (e) no dedicated audit-log entity for RFQ, unlike every other module so far — the owner's Part 9 bullet list doesn't ask for a "History" section the way Requisition's does. | RFQ Management (Part 9) | Open |
| 31 | Several Purchase Order decisions are frontend inventions, not owner-confirmed, even though the module's three top-level milestones ("two-level approval," "Release state," "Issue state") are owner-named: (a) the full 7-value `PurchaseOrderStatus` vocabulary between and around those milestones; (b) **one PO per distinct vendor within a finalized RFQ, not one PO per RFQ** — since RFQ Management (Part 9) selects a vendor per line, a single finalized RFQ can produce several POs; (c) both Release and Issue are gated by the same `po:issue` permission, since no separate `po:release` key exists — confirm whether these should be two distinct capabilities/roles; (d) the "email dispatch status placeholder" is mocked as instantly `"sent"` the moment a PO is issued, rather than modeling an async send/retry/failure flow; (e) terms & conditions/tax editing was kept inline on the PO detail page (matching RFQ's Part 9 inline tax-edit pattern) rather than a dedicated `/[id]/edit` route like Contract/Requisition use, since a PO's editable-while-draft/rejected surface is only two fields. | Purchase Orders (Part 10) | Open |
| 32 | Several Store Management decisions are frontend inventions, not owner-confirmed: (a) GRN's owner-named "Purchase request" field is modeled as a reference to the source `PurchaseOrder` (Part 10), not a separate `PurchaseRequisition` reference, and can only be created against an `issued` PO; (b) more than one GRN may be recorded against the same PO (split deliveries) with no check that received quantities, summed across GRNs, stay within what was ordered; (c) GRN was given a dedicated list/`new`/`[id]` route set rather than ACE's single-dialog pattern, since its line items need more room than a modal; (d) `StockEntry` is a manually-maintained daily ledger, not automatically populated from GRN receipts — `GRNLineItem`'s free-text description (copied from a PO line) doesn't map onto `StockEntry`'s closed `StockMaterial` reference list without a confirmed rule for translating one into the other; (e) `StockEntry.supplierName` is free text rather than a `Vendor.id` reference, since not every stock movement traces back to a formal vendor record; (f) Wastage's "materials procured above defined scope" categorization (see also #12) is applied by whoever logs the entry, with no automatic comparison against ACE/Requisition quantities; (g) the required Wastage "summary" view is computed client-side from the full entry list rather than via a dedicated backend aggregate endpoint. Separately: §6B of the owner requirements ("MR — Site Project Manager," material issuance requests flowing to stores) has no dedicated route anywhere in `docs/PART_PROMPTS.md`'s 20-part roadmap and is **not implemented** — `StockEntry.consumedToday` is the closest existing capture point for material leaving stock, but no formal request/approval workflow exists around it. | Store Management (Part 11) | Open |

| 33 | Several MRC decisions are frontend inventions, not owner-confirmed: (a) resolving #6 — customer acceptance happens in the customer portal only, never admin-side; (b) `MRCLineItem.source: "grn" \| "manual"` — a line either certifies a specific Part-11 `GRNLineItem` (snapshotted, quantity capped at what was received) or is typed straight onto the certificate with no GRN behind it, since the only demo project with a `customerId` has no GRN data and the project with real GRN data has no `customerId`; (c) `MRC.customerId` is chosen independently by whoever issues the certificate, the same decoupled-from-`projectId` shape as `Contract.customerId`/`Contract.projectId`, rather than auto-derived from `Project.customerId` — a real backend should decide whether these must match once Project↔Customer ownership (see #19) is real; (d) no dedicated audit-log entity, the same scope reduction already applied to RFQ and GRN (#30, #32), since the owner's Part 12 bullet list doesn't ask for a "History" section; (e) a single `mrc:issue` permission key gates every admin-side write action (create/edit/issue/withdraw) rather than separate keys per action, the same reuse already applied to PO's Release/Issue split (#31c); (f) `GRNDetail`'s "Record MRC" link is one-way and doesn't pre-seed the new MRC's lines from that specific GRN, and there's no reverse "which MRCs already certify this GRN's lines" listing. | MRC (Part 12) | Open |
| 34 | Several Project Schedule decisions are frontend inventions, not owner-confirmed — the owner requirements name "pro-rata progress-to-green visualization" and "delay-in-days reflection" without defining either formula: (a) "expected" progress is computed as straight-line (pro-rata) elapsed time between `plannedStart` and `plannedEnd`, compared against actual `executedQuantity / quantity`; (b) "delay in days" is simply how many days past `plannedEnd` today is, while the activity is still short of 100% — not a projection of a finish date from the activity's actual execution rate, which would need a separately-confirmed formula; (c) `ScheduleProgressEntry.executedQuantity` is a cumulative reading (the latest by `recordedAt` is "current progress"), not an incremental delta per entry, mirroring how `StockEntry`'s ledger fields work but logged as an append-only history instead of one mutable row; (d) no over-execution check — a reading above `quantity` isn't rejected, the same non-enforcement already applied to GRN's ordered-vs-received quantities (#32b); (e) the list has no `Pagination` control and defaults its sort to `sequence` ascending rather than `createdAt` descending like every other module, since the Gantt half of this view needs every activity in view together, not a paginated slice; (f) the project filter has no "All projects" option — mixing several projects' unrelated date ranges into one Gantt wouldn't read as a schedule for anyone. | Project Schedule (Part 13) | Open |

| 35 | Several DPR decisions are frontend inventions, not owner-confirmed: (a) `DPRWorkItemEntry.todayQty` is captured as an **incremental** delta ("today's" execution), not a cumulative as-of-date reading like `ScheduleProgressEntry.executedQuantity` (#34c) — the owner's table names "Today's Qty." and "Cumulative Qty." as two separate columns, so this build takes "today's" literally and computes previous/cumulative/%-complete at render time from every earlier `DPRWorkItemEntry` for the same work item across the whole project, never stored; (b) no enforced "at most one DPR per project per day" uniqueness — left to the backend, the same looseness already applied to GRN against a single PO (#32b); (c) `DPRWorkItemMaster` (the 77-item Civil/Electrical/Plumbing & Sanitary/Finishing list) is modeled as static frontend configuration (`src/lib/constants/dpr-work-items.ts`), not an adapter-backed entity with its own create/edit UI — the owner text's "configurable" is read the same way `StockMaterial`'s reference list was (#32d); (d) the DPR → Schedule Tracking push, when a work-item line carries a `scheduleActivityId`, records `executedQuantity = (that activity's own latest executedQuantity) + todayQty` as a new `ScheduleProgressEntry` — deliberately not the DPR line's own computed cumulative, to avoid silently regressing an activity's pre-existing Schedule progress the first time a DPR gets linked to it; a stale/cross-project link is skipped rather than failing the submission; (e) **fixed in the post-Part-20 stabilization pass, Phase 4, and further fixed in the BrickBasket final hardening pass** — editing an already-linked DPR line used to append a fresh auto-recorded entry every save instead of correcting the earlier one, compounding the activity's progress; fixed via `ScheduleProgressEntry.source` + `upsertProgressFromSource` (find-and-replace by a stable `workItemMasterId`+`location` key), a narrow exception to #34c/#34e's append-only design that leaves every manually-typed entry untouched. The Phase 4 fix still left a real gap — removing a linked line or repointing its `scheduleActivityId`/`workItemMasterId`/`location` on a later edit orphaned the earlier pushed entry, since there was no delete method — **now closed**: a new `ScheduleAdapter.removeProgressBySource` method removes exactly that orphaned entry, and `DPRAdapter.update()` reconciles the DPR's old vs. new set of linked `(activityId, sourceKey)` pairings on every edit. See `docs/CHANGELOG.md`, `docs/WORKFLOWS.md`'s Part 14 section, `docs/API_CONTRACTS.md`'s `PATCH /api/dprs/:id` section, and #55 below for the full reconciliation contract a real backend must implement transactionally; (f) `DPR` was given a dedicated list/`new`/`[id]`/`[id]/edit` route set (matching MRC/GRN) rather than Schedule's single-page pattern, since its two tables need more room than a `Dialog`; (g) the admin list has an "All projects" filter option, unlike Schedule's required single-project filter, since DPRs are naturally browsed across every project. | Daily Progress Report (Part 14) | (a)-(d) and (f)-(g) Open; (e) Resolved for this build (Phase 4 + BrickBasket final hardening pass) — see #55 for the backend-transaction contract this implies |
| 36 | Several Project Cost Accounting decisions are frontend inventions, not owner-confirmed: (a) `CostEntry.category` reuses the shared 8-value `ContractCategory` union rather than the narrower 7-value literal list the owner's Part 15 text names (civil, mechanical, electrical, plumbing & sanitary, finishing, labour, other — omitting `design_consultancy`), for the same internal-consistency reasoning as #23; (b) `actualAmount` is a manually-entered number on the form, not auto-aggregated from Payments/Receipts — that module (Part 16) doesn't exist yet, so there's no data to aggregate from; (c) `projectId` and `category` are both immutable after creation (together they identify the row), mirroring `ACEItem.projectId`'s and `StockEntry.projectId`/`material`'s "identity fields don't change" precedent; (d) the project summary's Contract-value cross-link and "Expected Profit" figure (contract value minus total actual spend) are a frontend invention with no owner-confirmed formula, computed by matching accepted `Contract`s to the project client-side (`ContractListParams` has no `projectId` filter) rather than a backend aggregate; (e) mock budget/actual figures are deliberately different from the owner's own example numbers (Civil ₹35L, Electrical ₹7L, etc.), per this part's explicit "illustrative, not real" instruction; (f) `CostEntry` was given ACE's single-list-page-plus-Dialog pattern rather than MRC/GRN/DPR's dedicated-route-set pattern, since a budget/actual/notes form fits comfortably in a Dialog. | Project Cost Accounting (Part 15) | Open |

| 37 | Several Payments/Receipts + Bank & Cash decisions are frontend inventions, not owner-confirmed: (a) the owner's single §8B bullet ("PO/vendor code/invoice details/invoice amount/invoice copy/payment details") is split into two entities, `Invoice` and `Payment`, rather than one combined record, so a single invoice can be settled across multiple payments — the same "split delivery" reasoning GRN applied to POs (#32b); (b) `Payment` is modeled as one `direction`-discriminated entity (`"payment"` vs `"receipt"`) rather than two separate entities, and the entire "receipt" side (customer-facing payments against a `Contract`) is invented outright — the owner text only confirms the vendor/PO side, but the module is named "Payments **& Receipts**" and this part's own blockquote requires a read-only `/dashboard/payments` customer view, which needs receipt data to show anything; (c) `PaymentMode`'s 5-value vocabulary (`bank_transfer`/`cheque`/`cash`/`upi`/`other`) is invented, with no owner-named list; (d) `Invoice.invoiceCopyFileName`/`invoiceCopyFileSizeBytes` are metadata-only — no real file bytes are stored, a scaled-down version of `Document`'s file handling (Part 6) without its versioning/preview machinery; (e) `BankTransaction` has **no confirmed relational model at all** — the owner's §8C bullet list is explicitly a flat "prepare fields for" list — so it's kept as its own flat ledger record with an *optional* `paymentId` cross-link added as a frontend addition, and every field (unlike `Invoice`/`Payment`) stays editable after creation since there's no confirmed identity to lock; (f) `itcApplicable` is modeled as a boolean rather than a named vocabulary; (g) `/admin/finance/payments` reuses Wastage's Detail/Summary toggle mechanic (#32g) to switch between two sibling record types (Invoices, Payments & Receipts) on one route, rather than giving each its own page. | Payments/Receipts + Bank & Cash (Part 16) | Open |

| 38 | Several Taxes/Fixed Assets/GSTR decisions are frontend inventions, not owner-confirmed: (a) `TaxRecord` and `FixedAsset` are both modeled as flat, fully-editable ledger records with no locked identity fields, the same no-confirmed-identity shape `BankTransaction` uses (#37e), since the owner's §8D/§8E text names each module's scope only (§8E also confirms the ₹5,000 minimum value, the one genuinely owner-confirmed rule across all three of this part's modules); (b) `TaxRecord.type` (`"tax"`/`"statutory_license_fee"`) is an invented discriminator with no owner-named vocabulary; (c) `FixedAssetCategory`'s 6-value vocabulary and the `active`/`disposed` lifecycle (with a `disposedAt` date) are both invented — the owner text names no categories and no disposal concept at all; (d) `GSTRRecord` is modeled as one `type`-discriminated entity (`"purchase"`/`"sales"`) rather than two, mirroring `Payment.direction` (#37b), since both categories share almost every field; (e) a `"purchase"` `GSTRRecord` **requires** a linked `GRN` (cross-adapter validated) because the owner text explicitly says "after stores GRN" — a stricter rule than `BankTransaction.paymentId`'s optional link (#37e), since here the owner text gives an actual rule to enforce; (f) a `"sales"` `GSTRRecord` requires *either* a linked, `status: "accepted"` `Contract` (the "constructed house" case) *or* a manual `saleDescription` (the "materials, if any" case) — enforced adapter-side in `GSTRAdapter.create`, mirroring how `PaymentAdapter.create`'s direction-branching validation was also enforced adapter-side (#37); (g) `GSTRForm` takes a **locked `type` prop** set by which tab's "Add" button opened it, rather than a switchable field like `PaymentForm`'s watched `direction` (#37b) — a deliberate simplification, since the page's Purchase/Sales tab already commits a new entry to a category before the form opens; (h) `GRNDetail`'s "Record GSTR Entry" link is one-way and doesn't pre-seed the new GSTR entry from that specific GRN, the same scope reduction already applied to MRC's "Record MRC" link (#33f). | Taxes, Fixed Assets, GSTR (Part 17) | Open |

| 39 | Several Cost-to-Complete decisions are frontend inventions grounded in, but not identical to, the owner's §8G derivation rules: (a) **Original Estimate (A) is sourced from `CostEntry.budgetAmount`** (Part 15) rather than a fresh `ACEItem.rate × quantity` total, because `ACEItem` (Part 8) carries no quantity at all — the owner's own §8G example figure (Original budget ₹77L) is identical to §8A's own example (Total Project Cost Budget ₹77L), strong evidence both name the same underlying figure; (b) **Completed till date (B) is sourced from `CostEntry.actualAmount` only** — the owner names "vendor invoices/project cost accounting" as two sources, but `Invoice` (Part 16) carries no `category` field, so attributing invoice amounts to a specific cost category would need the same unconfirmed ACE/RFQ/PO tracing chain used for "ordered" below; only the already category-scoped `CostEntry` figure is used; (c) "ordered but yet to be completed" is traced `PurchaseOrder.rfqId → RFQLine.aceItemId → ACEItem.category` across every non-`draft`/non-`rejected` PO — a 3-hop cross-adapter composition, the deepest in this app — and a PO line whose source requisition line was `"additional"` (free-entry, no ACE reference) cannot be traced, so its value is summed into a separate `unallocatedOrderedTotal` rather than folded into any category or dropped; (d) Balance to complete (C) = `max(0, ordered − B) + max(0, A − max(B, ordered))`, not the simpler `max(0, ordered − B) + max(0, A − ordered)` — the `max(B, ordered)` guard exists specifically to avoid double-counting categories where real spend already exceeds anything traceable through the ACE/RFQ/PO chain, which is most of this mock dataset's actual spend; (e) the owner's explicit "applies to service & composite works, not supply components" split for ordered-not-completed is **not implemented** — `ContractCategory` has no confirmed supply-vs-service/composite classification to split by; every category is treated uniformly; (f) `CostToComplete` has no persisted record, no mock data file, and no create/update form at all — it is a pure render-time report, the first entity in this app modeled that way; (g) `/admin/finance/cost-to-complete` has no "All projects" option, the same single-project-scoping decision Schedule made (#34f); (h) `/admin/cost-management` was additionally wrapped in a `PermissionGuard` for `cost_management:view` (a route-level consistency change, not a business-logic one) — every other implemented route in this app enforces its permission boundary at the route, and a placeholder shouldn't be the one exception. | Cost-to-Complete + Cost Management (Part 18) | Open |

| 40 | Several Cross-Module Polish decisions are frontend inventions, not owner-confirmed: (a) **Notifications and Ops Dashboard metrics are pure computed aggregates, no persisted record** — extending `CostToComplete`'s Part 18 precedent to two more entities rather than inventing a notification-events table or a backend push mechanism, since no such thing exists yet; this also resolves #10 for the frontend build as in-app-only, computed live, never persisted; (b) the exact permission → condition → message rule table (`NotificationsAdapter`/`OpsMetricsAdapter`'s file-level comments) is a frontend invention sized specifically so every one of the 8 demo personas sees at least one real, live notification/metric from their own permission set — the owner requirements name none of these specific triggers; (c) `OpsMetricsAdapter` imports `mockProjects` directly (for the "Projects Over Original Estimate" metric's per-project loop) rather than inventing a `projectsAdapter` — Projects have no adapter anywhere in this app, and every component needing the project list already imports `mockProjects` directly (e.g. `AdminCostToCompletePage`, Part 18); (d) `StatCard` and `MetricCard`, named separately in the master prompt's core-component list with no distinguishing description, were consolidated into one `MetricCard` primitive rather than building two near-duplicates; (e) **Global Search is scoped to exactly the entities with a real `[id]` detail route** — Contract/Vendor/Requisition/RFQ/PurchaseOrder/GRN/MRC for staff, Contract/MRC for the customer persona — deliberately excluding Lead (detail-drawer-only, no dedicated route) and Project (no detail route at all yet); each entity reuses its own adapter's existing `search` param rather than a new search index/endpoint; (f) `GlobalSearch` is hand-rolled rather than built on the existing `Dialog`/`Sheet` primitives, since neither's fixed-width, click-to-close interaction model supports the wider centered panel and real arrow-key result navigation this needs; (g) **`AuditTimeline<Entry>` consolidates 4 previously-duplicated components** (`ContractAuditHistory`/`POAuditHistory`/`RequisitionAuditHistory`, found byte-for-byte identical in markup, plus `LeadActivityLog`'s read-only half) — `VendorAssessmentHistory`/`DocumentVersionHistory` were checked and are genuinely different shapes, deliberately not folded in; (h) the accessibility/responsive/performance "full pass" named in this part's own blockquote turned out to be substantially already satisfied — `globals.css`'s `prefers-reduced-motion` rule and global `:focus-visible` ring both date to Part 1 — narrowing this part's real remaining work to a skip-to-content link, an icon-button `aria-label` audit (zero violations found), a mobile-fallback spot-check (every Part 15–18 dense table already uses `DataTable`), and a WCAG contrast spot-check; (i) **the contrast spot-check found `ink-muted` (~3.6–4.0:1) and the `warning`/`success` semantic tokens (~2.2–2.5:1 as text) fall short of the 4.5:1 AA threshold for normal-sized text**, while the confirmed brand colors (`brand-red`, `ink`) both clear it (4.3–10.7:1) — per this part's own scope, confirmed brand colors were left untouched and this finding is flagged here rather than silently fixed globally, since a token-wide change would ripple across every screen built in Parts 3–18; `MetricCard`'s own "attention" tone was adjusted (value text stays `text-ink`, only the icon chip carries the `warning` tint) so this part's new component doesn't add a fresh instance of the low-contrast pattern; confirm whether `--ink-muted`/`--color-warning`/`--color-success` should be darkened app-wide, and if so, in which part. | Cross-Module Polish (Part 19) | Open |

| 41 | Several Part 20 documentation findings surfaced decisions and gaps that were never logged when the code shipped: (a) there is no toast/snackbar system anywhere in this app — every error and success outcome renders in place (`ErrorState`, an inline form banner, or a status-driven UI change), a frontend scoping decision made across Parts 3–19 without ever being written down — still Open, unchanged; (b) **fixed in the post-Part-20 stabilization pass, Phase 12**: Stock (`stock-form.tsx`, Part 11) and Wastage (`wastage-form.tsx`, Part 11) both collect an `otherMaterialName` field that's only actually required when `material === "other"`, but neither form's zod schema enforced that conditionally — now both use `.superRefine()`, matching MRC's `source`/DPR's work-item "Other" branch/GSTR's `type`/Payment's `direction`; (c) **fixed in the same Phase 12**: Requisition (`requisition-form-schema.ts`, Part 8) now requires `aceItemId` via `.superRefine()` when a line's `source === "predefined"`, matching MRC's equivalent `grnLineItemId`-required-when-`source === "grn"` branch; (d) no client-side code anywhere branches on a numeric HTTP status code — every adapter failure is a plain `throw new Error(message)` and every catch block just displays `.message`, so there is no implemented frontend behavior yet for a structured `422` validation-error body, a `401` triggering forced logout, or any retry/backoff strategy beyond a manual user-clicked retry — a real REST adapter layer (`docs/API_INTEGRATION_GUIDE.md`) is expected to add this, not a preserved frontend contract — still Open, unchanged; (e) confirmed via direct inspection of `package.json` and the repository tree that **no test framework or test files exist anywhere in this codebase** — **partially addressed in the post-Part-20 stabilization pass, Phase 18** (`vitest.config.ts` plus 7 pure `*-math.ts` unit test files) **and further addressed in the BrickBasket final hardening pass**, which added 9 more adapter-workflow test files covering the 11 highest-value workflows (Contract, Requisition, RFQ, PO, Store MR, DPR×3, GRN, MRC, Cost-to-Complete — see `docs/TESTING.md`) — 17 test files total now. Component tests (the form layer) and e2e workflow tests still remain untested, and no test file from any pass has ever actually been run in this sandbox (no npm registry access, unchanged), so all 17 are unverified-by-execution the same way every other change in every pass is. | Documentation (Part 20) — see `docs/VALIDATION_RULES.md`, `docs/ERROR_HANDLING.md`, `docs/TESTING.md` | (a) and (d) Open; (b) and (c) Resolved (Phase 12); (e) Partially resolved (Phase 18, extended BrickBasket final hardening pass) — component/e2e tests still Open |

| 42 | The first real `npm run dev` run (post-Part-20) surfaced a runtime-only bug no static verification in this project could have caught: `admin/layout.tsx` and `dashboard/layout.tsx` (both Server Components, no `"use client"`) imported `ADMIN_NAV`/`PORTAL_NAV` and passed them as a `navGroups` prop into `AppShell` (a Client Component). `NavItem.icon` holds a raw lucide-react component (function) reference, and React Server Components cannot serialize a function — or any object holding one — across the Server→Client prop boundary; this throws "Functions cannot be passed directly to Client Components" / "Only plain objects can be passed to Client Components from Server Components" at runtime. This exact class of bug is invisible to `verify.py`/`verify_imports.py`/`verify_use_client.py`, none of which can run `tsc`/`next dev` in this sandbox — it only surfaces by actually starting the dev server, which only became possible once the user ran it locally. | App Shell / Navigation (Part 3, fixed post-Part-20) | Resolved: `AppShell` now imports `PORTAL_NAV`/`ADMIN_NAV` itself and picks the right one from its own `variant` prop, so the icon-bearing config is constructed inside the Client Component and never crosses the boundary as a prop; both layouts dropped the `navGroups` prop and their now-unused nav-config import. Also fixed in the same pass: `src/app/layout.tsx`'s `themeColor` was still inside the `metadata` export (a Next.js warning, "Unsupported metadata themeColor...") — moved to its own `viewport` export, the current Next.js convention. See `docs/CHANGELOG.md` and `docs/COMPONENT_GUIDE.md`. |

| 43 | Does the admin "Send for Acceptance" action on a Contract constitute BrickBasket's own internal approval of the contract terms, or only the customer-facing send step (with a separate, unmodeled internal approval expected to happen before or alongside it)? The owner requirements only name "review/accept" from the customer's side; this build's `sent_for_acceptance` → `accepted`/`declined` state machine has no internal-approval step distinct from "send." | Contract Management (Part 5) — see `docs/WORKFLOWS.md` | Open — flagged, not resolved; three plausible readings are named in `docs/WORKFLOWS.md` without picking one, per this stabilization pass's explicit instruction not to silently resolve it |
| 44 | Post-Part-20 stabilization pass audit found `Project` (`src/types/domain/project.ts`) was the one entity in this app with **no adapter at all** — 69+ call sites across admin and portal screens imported `mockProjects` directly from `@/data/mock/projects`, bypassing the Component → hook → adapter boundary every other entity follows. A `ProjectsAdapter`/`useProjects`/`useProject` seam was added (list/get only — nothing in the app creates or edits a Project yet) and a representative set of call sites migrated onto it. The remaining direct-import call sites are not yet migrated — no `tsc`/build is available in this sandbox to safely verify a full mechanical sweep (see `docs/CHANGELOG.md`'s stabilization-pass entry), so the remainder is tracked here as explicit follow-up rather than risked as an unverified blind edit. | App-wide (originally Part 3's `mockProjects`, surfaced during this stabilization pass) | Open — follow-up migration, not a business question |
| 45 | Post-Part-20 stabilization pass audit found mutation call sites (Contract/MRC/Requisition/RFQ/Document detail pages) where a failed mutation's return value was never checked before closing a confirmation dialog or navigating away — since this app's mutation hooks catch their own errors and resolve to `null`/falsy rather than throwing, a rejected action (e.g. a backend refusing "Send for Acceptance") looked identical to a successful one. Fixed by guarding on the mutation's return value before any success-path side effect, and by adding an optional `error` prop to `ConfirmationDialog` so a failure shows inline instead of the dialog silently closing. PO/GRN/DPR/Vendor/Finance/Bank/Payments/Invoice/Stock/Wastage/Schedule modules were audited and found already clean. | Contracts, MRC, Requisitions, RFQ, Documents (Parts 5/8/9/12/6, fixed during this stabilization pass) | Resolved for this build — see `docs/CHANGELOG.md` |
| 46 | Post-Part-20 stabilization pass corrected several public marketing-site content issues: (a) the Portfolio page listed 4 projects with no confirmed backing data — removed, now sourced from the same confirmed project list Home uses; (b) the Services page named "Post Construction Support" as a live, unqualified service — now shown separately as pending confirmation; (c) service names across Home/Services/Plans/Footer had drifted from the owner's exact confirmed naming — centralized into one `src/lib/content/public-site.ts` module so they can't drift again; (d) Contact/Footer's social icons linked to `href="#"` (dead links, no confirmed URLs) — removed rather than guessed; (e) Contact's "We typically respond within 24 hours" was an unconfirmed SLA claim — reworded; (f) all placeholder imagery now goes through one documented `ImagePlaceholder` component instead of ad-hoc divs, with a commented-out real `next/image` swap-in path ready for real photography. | Public website — Home/Portfolio/Services/Plans/FAQ/Contact/Footer (Parts 1–2, fixed during this stabilization pass) | Resolved for this build — real photography, confirmed social URLs, and "Post Construction Support" scope/pricing still need client input |
| 47 | Post-Part-20 stabilization pass found every customer-portal list page (`/dashboard`, `/dashboard/projects`, `/dashboard/contracts`, `/dashboard/mrc`, `/dashboard/payments`) scoped its data as `customerId: session?.user.id` — and every adapter behind those pages follows the same `if (params.customerId) filter(...)` convention, where an **absent** `customerId` deliberately means "no filter, return everything" (needed for admin "list all" screens). Since `session` is `null` during the auth provider's initial loading tick and while unauthenticated, `session?.user.id` evaluates to `undefined` for that brief window — meaning every one of these five pages was briefly listing **every customer's** projects/contracts/MRCs/payments, not just the signed-in customer's. Fixed with one shared `useCustomerId()` hook (`src/hooks/use-customer-id.ts`) used consistently across all five pages instead of independent inline guards. `PermissionGuard` (route-level authorization) does not prevent this — it is a data-scoping gap, not an access-control one. | Customer Portal — Dashboard/Projects/Contracts/MRC/Payments (Parts 3/5/12/13/16, fixed during this stabilization pass) | Resolved for this build — confirm the real backend's customer-facing endpoints always require and enforce a customer id server-side, never accepting "list all" from a customer-authenticated request |
| 48 | Post-Part-20 stabilization pass accessibility fixes to shared primitives, none owner-specified but all real WCAG gaps: (a) `Dialog`/`Sheet` had no focus trap, no initial focus on open, and no focus restoration on close — fixed via one shared `useFocusTrap` hook, plus switching both from `aria-label={title}` to a proper `aria-labelledby`/`aria-describedby` pair; (b) `FormField` computed `errorId`/`hintId` but never wired them to the control it wraps — fixed by cloning the single child control to inject `aria-describedby` automatically; (c) `DataTable`'s sortable column headers conveyed sort direction only visually — added `aria-sort` (`ascending`/`descending`/`none`) and `scope="col"`; (d) `ProjectSelector`'s trigger+listbox markup was already correct ARIA-wise but had no keyboard behavior — added Arrow Up/Down open-and-navigate (wrapping), Home/End, and Escape-closes-and-refocuses-trigger, the standard listbox-with-roving-focus pattern. | Shared UI primitives — Dialog/Sheet/FormField/DataTable/ProjectSelector (Parts 1/3/4, fixed during this stabilization pass) | Resolved for this build — see `docs/CHANGELOG.md` |
| 49 | Post-Part-20 stabilization pass built the **Store Material Requisition ("MR", §6B)** module (Phase 2) from a single owner sentence ("Site/Project Manager requests material already in stock; flows to Stores for issuance") — every vocabulary/permission/scope decision is this frontend's own, not owner-confirmed: status vocabulary `draft→submitted→approved/rejected→issued` (mirroring Purchase Requisition's Part 8 shape plus a terminal issue step); `store_requisitions:read/create/action` permission keys and their role assignment (project_manager/site_engineer request, store_personnel/admin decide+issue); no dedicated audit-log entity (unlike Purchase Requisition's `RequisitionAuditEntry`); and — most consequentially — **`issue` does not write a `StockEntry` row**, so this frontend does not simulate the actual stock decrement a real issuance would cause. Same pass also fixed a real correctness bug in the pre-existing DPR→Schedule linkage (Phase 4): re-editing a DPR was re-adding its work-item progress to the Schedule ledger on every save instead of correcting the same entry, compounding percent-complete on each edit. Fixed via a narrow, deliberate exception to the Schedule module's append-only ledger — only entries carrying a `source: { type: "dpr", key }` marker can be found-and-corrected (`upsertProgressFromSource`); manually-entered progress remains strictly append-only. | Store Material Requisition module (new, this pass) / DPR↔Schedule linkage (`src/lib/api/adapters/dpr-adapter.ts`, `schedule-adapter.ts`, Parts 13–14, fixed this pass) | Store Requisition: Open — needs owner/backend confirmation of the status vocabulary, the no-audit-log decision, and whether issuance should decrement `StockEntry` server-side. DPR/Schedule fix: Resolved for this build — see `docs/CHANGELOG.md` |

| 50 | Post-Part-20 stabilization pass added a monthly planned-quantity breakdown to Project Schedule (Phase 3), a frontend invention on top of #34's existing gaps: `ScheduleActivity.monthlyPlan` is optional per activity (`[]` = "not broken down," always valid), and once any month is filled in, entries must sum to exactly the activity's `quantity` — enforced both in the form and in the adapter. Left unconfirmed: (a) whether every activity should eventually require a monthly breakdown, rather than it being opt-in; (b) whether a *partial* breakdown (some but not all spanned months filled in, summing to less than the total) should be allowed as a valid in-progress state rather than rejected the same as any other total mismatch; (c) whether monthly figures should feed into `expectedPercent`/`delayDays` (a month-by-month pro-rata expectation) instead of the existing straight-line whole-activity formula — this build keeps the monthly plan purely informational, not wired into the progress/delay math. Also added in the same phase, not owner-related but real fixes: a visible delay-in-days display (`getDelayDays` was computed since Part 13 but never actually shown anywhere) now appears in the schedule table, the Gantt chart, and the progress sheet; and a bulk `listProgressForActivities` adapter method replaces the schedule page's previous N+1 `Promise.all`-of-`listProgress` pattern. | Project Schedule (Part 13, extended Phase 3) | Monthly plan: Open — needs owner confirmation per (a)-(c) above. Delay display / bulk progress fetch: Resolved for this build — see `docs/CHANGELOG.md` |

| 51 | Post-Part-20 stabilization pass (Phase 15–17) documented, rather than changed, how the finance modules (Parts 15–18) relate: `docs/ARCHITECTURE.md`'s cross-module data-flow section named `Project Cost Accounting → Payments → Cost-to-Complete` as a chain, which reads as if figures flow between those modules — they don't in this build. `CostEntry.actualAmount` (Part 15) is manually typed, never aggregated from `Payment`/`Invoice` (Part 16, built later, never retrofitted to feed Part 15); `TaxRecord`/`FixedAsset`/`GSTRRecord` (Part 17) are independent of both; `CostToComplete` (Part 18) is the one module that cross-composes others, and even it only reaches into Parts 8–10 and 15, never Parts 16–17. New `docs/ARCHITECTURE.md` §D2 is the consolidated authority table so a future session doesn't wire these together on its own inference. Whether any of these should actually be linked (e.g. `CostEntry.actualAmount` sourced from `Payment` totals instead of manual entry) is unconfirmed and unchanged by this pass — see #36b, #37, #39. Also restates, as an explicit standing guardrail rather than a new decision: `/admin/cost-management` (Part 18) must stay a placeholder with no invented business rules until Pushkar Tiwari's Excel spec is delivered — this was already true from Part 18 onward, just not previously written as a guardrail for future sessions to see before touching that module. | Finance modules generally (Parts 15–18) | Documentation-only — no code change; the underlying module-independence questions (#36b, #37, #39) remain Open |

| 52 | Post-Part-20 stabilization pass (Phase 21) added a REST client foundation (`src/lib/api/http-client.ts`) ahead of any real backend to point at — nothing imports it yet. Frontend inventions baked into it, none owner- or backend-confirmed: (a) the assumed error-body shape from a real backend is `{ message: string }` (matching the `ErrorResponse` schema already used throughout `docs/openapi.yaml`) — a real backend's actual error envelope may differ, in which case only this one file's error-parsing branch needs to change, not every adapter; (b) `credentials: "include"` (cookie-based auth) is the default, matching one of the two live options `docs/FRONTEND_BACKEND_HANDOFF.md` names, but a bearer-JWT backend would need callers to add an `Authorization` header per request instead — the helper doesn't do this automatically; (c) whether the three pure-aggregate adapters (`CostToComplete`/`Notifications`/`OpsMetrics`) end up calling this same helper (composing several REST calls) or a backend adds a dedicated aggregate endpoint each is unchanged from `docs/API_INTEGRATION_GUIDE.md`'s existing open call-out — this phase didn't need to decide it either. | REST client foundation (`src/lib/api/http-client.ts`, new this pass) | Open — none of (a)-(c) can be confirmed without an actual backend to test against |

| 53 | Post-Part-20 stabilization pass (Phase 23, final quality-gate scans) found and fixed one real mock-data defect: `store-requisitions.ts` (new this pass, Phase 2/#49) reused the literal IDs `mr_1`..`mr_5`, already used by the unrelated, pre-existing `material-requirements.ts` (`MaterialRequirement`, a Purchase/Material Requisition's own sub-line-items — Part 8). Confirmed via grep that nothing referenced those specific `StoreRequisition` ID values outside their own mock file (safe to rename) and that the two entity types are never rendered in the same list/keyed structure today (so this caused no visible symptom yet) — but a shared ID namespace between two unrelated entities is exactly the kind of latent collision that turns into a real bug the moment either type is combined with the other (a unified cache, a future Global Search entry — see #40e — or any lookup keyed by raw `id` instead of `type+id`). Fixed by renaming `StoreRequisition`'s IDs to the distinct `storereq_1`..`storereq_5` prefix. The rest of Phase 23's static sweep (brace/paren balance across all 415 `src/` files, `@/` import resolution, `"use client"` directive consistency, a targeted re-check of the `nav-config.ts`-class RSC serialization bug across every other `icon:`-bearing config in the app, permission-key-vs-`PermissionKey`-union consistency, and full cross-file mock-data foreign-key resolution) found zero further issues — see `docs/CHANGELOG.md` for the full scan inventory. | Mock data — `store-requisitions.ts` (Phase 2/#49, fixed Phase 23) | Resolved for this build — see `docs/CHANGELOG.md`. Purely a frontend mock-data hygiene fix; nothing here needs owner/backend input. |

| 54 | BrickBasket final hardening pass: `StoreRequisitionsAdapter.issue()`'s mock implementation only flips `status`/`issueDate` on the in-memory record — it does not (and, being an in-memory array with no real stock ledger, cannot meaningfully) verify real-time stock availability, write a stock-consumption/movement record, or persist the authenticated actor who performed the issuance (the type has no field for one). This is now explicitly documented, not silently left as a gap, as an 8-step atomic backend transaction requirement in `docs/API_CONTRACTS.md`'s `/issue` section, referenced from `docs/DATA_MODELS.md` (the missing `issuedBy`/`issuedByName` field question) and `docs/WORKFLOWS.md`. **The frontend must never become the authority for stock truth** — no client-side prediction of issuance success against current stock should ever be added; that check belongs entirely to the real backend transaction. Open questions for backend/owner: (a) should `StoreRequisition` gain `issuedBy`/`issuedByName` fields, or is the actor captured only in a backend-side stock-movement/audit record with no frontend-visible equivalent; (b) what should the specific `409` message/code be when stock has been consumed by something else between approval and issuance (a race this frontend's single-user mock can't simulate at all). | Store Material Requisition — `issue()` (`src/lib/api/adapters/store-requisitions-adapter.ts`, Phase 2/#49, contract documented this pass) | Backend-required — not implementable as a real transaction in this mock-only frontend; (a) and (b) need backend/owner input |

| 55 | BrickBasket final hardening pass: DPR → Schedule progress reconciliation. The Phase 4 fix (#35e) corrected an in-place edit of an already-linked line, but left four related cases as known, undocumented-as-fixed gaps — a linked line deleted from a DPR on a later edit, or its `scheduleActivityId`/`workItemMasterId`/`location` changed — each of which orphaned the line's previously-pushed `ScheduleProgressEntry` on whichever activity it was originally pushed to. All four are now handled: a new `ScheduleAdapter.removeProgressBySource` method removes exactly the one matching `source`-tagged entry (never a manually-typed one), and `DPRAdapter.update()` computes the DPR's old vs. new set of linked `(activityId, sourceKey)` pairings on every edit and removes whichever old pairing has no surviving match, before pushing the new set via the existing `upsertProgressFromSource`. This is a genuine frontend mock-adapter fix, not merely documentation — but it operates on a plain in-memory array with no real transaction primitive, so "remove old entries, then push new ones" happens as two sequential in-memory operations, not one atomic commit. **The corresponding backend requirement, not yet built anywhere**: a real `PATCH /api/dprs/:id` must perform the DPR write and the full Schedule-contribution reconciliation (determine old vs. new linked triples → remove orphaned entries → create/update surviving ones) as a single database transaction — see `docs/API_CONTRACTS.md`'s `PATCH /api/dprs/:id` section for the full step-by-step contract. Open for backend/owner: whether a real backend should additionally keep its own explicit audit trail of these reconciliation removals (this frontend's mock has none, matching DPR's existing no-audit-log scope decision, #35/#43), and whether concurrent edits to the same DPR (two people editing simultaneously) need optimistic-concurrency protection beyond what this single-user mock can simulate. | DPR ↔ Schedule reconciliation (`src/lib/api/adapters/dpr-adapter.ts`, `schedule-adapter.ts`, Part 13-14, fixed this pass) | Frontend mock fix: Resolved for this build. Backend transaction contract: Backend-required — needs a real database transaction, not implementable as a true atomic operation in this mock-only frontend |

| 56 | Home hero's second CTA was labeled "Watch Video" with a play icon but always linked to `/how-it-works` (a text/step page) — no video asset existed in this codebase or the owner material at the time. Relabeled "How It Works" so the button matched its real destination rather than fabricating a video. The owner has since supplied a real explainer video (Google Drive) — wired in via `INTRO_VIDEO` (`src/lib/content/public-site.ts`) and `WatchVideoButton` (`src/components/marketing/watch-video-button.tsx`); the hero button is back to "Watch Video" and opens the real video in an in-page modal. Fixing this surfaced that `/how-it-works` then had no link anywhere in the site at all — see #57, resolved in the same pass. | Public Home hero (`src/app/(public)/page.tsx`) | Resolved for this build. One dependency outside frontend control: the Drive file's sharing must stay set to "Anyone with the link" (Viewer) or visitors see a Google "You need access" screen instead of the video — please confirm/keep that setting on the file |

| 57 | `/how-it-works` was a fully-built page (6-step process explainer) with no link to it anywhere in the site's navigation — not `MAIN_NAV`, not the footer, not any other page — reachable only via the Home hero's old "Watch Video" button. Once that button was fixed to open a real video instead (#56), the page would have become permanently unreachable, so it's now a proper one-pager section (`HowItWorksSection`) placed between Services and Projects, added to `MAIN_NAV` and the footer's Quick Links. Content itself is unchanged and was already flagged as a frontend editorial narrative, not an owner-confirmed workflow (see the section's own header comment). | Public site navigation — `MAIN_NAV`/footer (`src/lib/constants/routes.ts`, `src/components/marketing/footer.tsx`), new `src/components/marketing/sections/how-it-works-section.tsx` | Resolved for this build (navigation gap fixed). The step content itself remains frontend-authored, not owner-confirmed — same open item as before |

Update this file the moment any answer arrives — do not silently resolve elsewhere in code or docs without a corresponding row update here.
